# grounded-mcp
"Expose your notes over MCP" servers are everywhere. None ship with proof of trustworthiness. This is an MCP server that ships with its own eval suite - retrieval quality, abstention correctness, and entitlement leakage as CI-gated numbers.
